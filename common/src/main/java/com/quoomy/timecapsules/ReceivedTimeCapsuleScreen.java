package com.quoomy.timecapsules;

/*
import net.minecraft.client.gui.screen.Screen;
import net.minecraft.client.texture.DynamicTexture;
import net.minecraft.text.Text;

public class ReceivedTimeCapsuleScreen extends Screen
{
    private final TimeCapsuleData data;
    private DynamicTexture imageTexture;
    private boolean largePreview = false;

    public ReceivedTimeCapsuleScreen(TimeCapsuleData data)
    {
        super(Text.of(data.getNameOrSignature() + "'s Time Capsule"));
        this.data = data;
    }

    @Override
    protected void init()
    {
        super.init();

        if (data.getImage() != null)
        {
            this.imageTexture = new DynamicTexture(data.getImage());
        }
    }
}
 */